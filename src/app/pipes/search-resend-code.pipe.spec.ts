import { SearchResendCodePipe } from './search-resend-code.pipe';

describe('SearchResendCodePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchResendCodePipe();
    expect(pipe).toBeTruthy();
  });
});
