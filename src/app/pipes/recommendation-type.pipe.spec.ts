import { RecommendationTypePipe } from './recommendation-type.pipe';

describe('RecommendationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
