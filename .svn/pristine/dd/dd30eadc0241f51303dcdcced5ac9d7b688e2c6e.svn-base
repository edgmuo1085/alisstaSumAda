import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { SelectRegisterEventPage } from './select-register-event.page';

describe('SelectRegisterEventPage', () => {
  let component: SelectRegisterEventPage;
  let fixture: ComponentFixture<SelectRegisterEventPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SelectRegisterEventPage ],
      imports: [IonicModule.forRoot()]
    }).compileComponents();

    fixture = TestBed.createComponent(SelectRegisterEventPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
