import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { InactiveUsersTalkComponent } from './inactive-users-talk.component';

describe('InactiveUsersTalkComponent', () => {
  let component: InactiveUsersTalkComponent;
  let fixture: ComponentFixture<InactiveUsersTalkComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InactiveUsersTalkComponent ],
      imports: [IonicModule.forRoot()]
    }).compileComponents();

    fixture = TestBed.createComponent(InactiveUsersTalkComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
